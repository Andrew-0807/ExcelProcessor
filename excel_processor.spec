# -*- mode: python ; coding: utf-8 -*-

block_cipher = None


a = Analysis(
    ['launcher.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('templates', 'templates'),
        ('static', 'static'),
        ('assets', 'assets'),
        ('processors', 'processors'),
    ],
    hiddenimports=[
        'pandas',
        'openpyxl',
        'openpyxl.cell._writer',
        'openpyxl.styles',
        'werkzeug.serving',
        'pystray',
        'PIL',
        'PIL._tkinter_finder',
        'requests',
        'packaging',
        'processors.borderou.main',
        'processors.borderou.borderou_to_import_transformer',
        'processors.borderou.csv_cleaner',
        'processors.borderou.csv_to_xlsx_converter',
        'processors.borderou.to_csv',
        'processors.cardcec.CardCec.pos_processor',
        'processors.cardcec.CardCec.view_output',
        'processors.cardcec.CardCec.xlsx_to_csv',
        'processors.core.classes.valoare_sgr',
        'processors.core.classes.valoare_minus',
        'processors.core.classes.format_add_column',
        'processors.core.classes.excel_data_extractor',
        'processors.core.classes.excel_processor',
        'processors.sales_transform.sales_transform',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=['torch', 'torchvision', 'torchaudio', 'tensorflow', 'matplotlib', 'scipy', 'pytest', 'unittest'],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)
pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)
exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='ExcelProcessor',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    icon='assets/icons/excel-processor-icon.ico',
)